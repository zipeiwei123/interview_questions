"""If burst ballons i , get coins nums[i-1]*nums[i]*nums[i+1]
    del num[i]"""

class Solution:
    def maxCoins(self, nums: List[int]) -> int:
        maximum_coins = [0]*(len(nums)+1)
        for index in range(len(nums)):
            
        
        return max(maximum_coins)